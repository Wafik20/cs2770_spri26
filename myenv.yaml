name: myenv
channels:
  - conda-forge

dependencies:
  - python=3.10
  - numpy
  - matplotlib
  - opencv
  - pillow
  - scipy
  - ipython
  - jupyterlab

  # PyTorch stack (must be together)
  - pytorch
  - torchvision

  - ipykernel
  - pip
  - pip:
      - imageio